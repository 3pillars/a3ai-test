
## Cron/loop hardening (Jun 17) — Telegram delivery + DaveH dedup
Acting on the "design loops, not prompts" article. Diagnosed via cron list:
- **Delivery bug (B):** jobs with explicit `to:"5157095960"` delivered fine; jobs using `channel:"last"` or missing `to` resolved "from last session" and failed (`not-delivered`). The "connection refused" errors came from `daveh_target_monitor.py`'s `_send_telegram()` POSTing to a dead localhost webhook; "chat not found" came from the in-job agent self-sending to a guessed target (@jacob).
  - FIX: patched `news:iran-update`, `Daily News Debate`, `mindset:profit-reminder` → explicit `{announce, telegram, 5157095960}`. Verified: mindset manual run went not-delivered → **delivered**.
- **DaveH spam (A):** script alerted every 4h whenever SMH in 95–105% band, no memory → fired identical alert for ~6 days incl. weekends (stale previousClose).
  - FIX: rewrote `scripts/daveh_target_monitor.py` with state file `daveh_state.json` — only emits 🚨 when the in-band ticker SET changes (entrant/exit), silent otherwise. Removed broken `_send_telegram()`. Rewired cron 6233f215 → delivery `none` + agent relays only the 🚨 block via message tool (explicit numeric ID), mirroring the working ipo-top-signal-monitor pattern. Tested: silent on no-change, clean alert on change.
- Remaining minor: iran-update / Daily News Debate payloads still say "send via message tool" AND now have announce → possible double-send (self-send likely still fails on bad target, so usually single). Optional follow-up: reword payloads to rely on announce only.
